Hi Claude, please implement Subject Effects for our isolated subject (processedBitmap) in the Jetpack Compose image editing application. These effects will turn the subject into a true sticker and make it look more artistic.1. Requirements for Stroke/Outline (Sticker Effect)Core Logic: Draw a solid outline wrapping around the outer boundaries/edges of the transparent subject bitmap.Implementation: Use Android's Paint with Style.STROKE and set a PathEffect or extract the alpha channel mask of the bitmap, dilate/expand it, and fill it with the selected stroke color before drawing the subject on top.UI Controls: * A Slider to adjust the outline thickness (from 0px to 30px).A Color Row to change the outline color (White, Black, Neon Yellow, Neon Pink, etc.).2. Requirements for Drop Shadow (3D Depth Effect)Core Logic: Add a soft blur shadow beneath or behind the subject.Implementation: Use Paint.setShadowLayer(radius, dx, dy, color) when drawing the bitmap on the Canvas. Ensure hardware acceleration compatibility or draw a blurred alpha mask offsetted by $(dx, dy)$.UI Controls: A Slider to control the shadow's blur radius/opacity to simulate depth.3. Requirements for Color Filters & AdjustmentsCore Logic: Modify the brightness, contrast, and saturation of the subject bitmap so it matches the lighting of the new background.Implementation: Use a ColorMatrix and apply it to the Paint object via ColorMatrixColorFilter when rendering the bitmap.UI Controls: Simple sliders for [Brightness], [Contrast], and [Saturation] (ranging from -1.0f to 1.0f or standard percentage scales).4. Preview & Export IntegrationAll effects must render smoothly on the preview screen's Canvas.Update the generateFinalBitmap function so that when the user exports the image, the final High-Res Bitmap includes the applied Shadow $\rightarrow$ Stroke Outline $\rightarrow$ Color Filtered Subject in the correct drawing order.5. OutputProvide clean, modular, and production-ready Kotlin code (ViewModel states and Composable UI components). No long explanations, just give me the code.