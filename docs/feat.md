This will allow users to save their current editing progress and resume editing later without losing their brush paths or slider settings.1. Data Layer & Storage (Room Database)Create a Room entity named ProjectDraft:id: String (Primary Key).timestamp: Long.sourceImageUri: String (To reload the originalBitmap via ContentResolver).sourceMimeType: String.serializedEditorState: String (Convert all EditorState knobs like feather, outline, shadow, contrast, saturation, and brightness into a JSON string via Gson/KotlinX Serialization).serializedTouchPaths: String (Serialize the entire list of TouchPath objects, including path coordinates/points, brush sizes, and modes so they can be reconstructed).2. ViewModel & State IntegrationIn RembgViewModel, handle loading a draft:Retrieve the ProjectDraft from the database.Reload originalBitmap from the sourceImageUri.Reconstruct the list of TouchPath and restore all EditorState values.Trigger recomposeSubject asynchronously on Dispatchers.Default to rebuild the 3-layer bitmap pipeline (workingBitmap $\rightarrow$ displayBitmap $\rightarrow$ effectedBitmap) so the preview restores exactly where the user left off.3. UI Changes (HomeScreen & EditorScreen)In EditorScreen: Add a "Save Draft" button in the TopAppBar. Clicking it will trigger the background save task and show a Toast notification.In HomeScreen: Add a "Recent Drafts" section (using LazyRow or LazyColumn) showing a list of saved drafts with their timestamp. Clicking on a draft item will transition the RembgUiState to load that specific draft session into the editor.4. OutputProvide clean, modular, and production-ready Kotlin code (Room Dao/Entity, ViewModel state logic, and Composable UI updates). No long explanations, just give me the code.