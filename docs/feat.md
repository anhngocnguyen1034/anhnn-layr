Hi Claude, please implement the Text Sticker & Typography feature for our app based on the architecture specified in feat.md. 1. Feature Description Allow users to overlay text (Meme captions) on top of their stickers, customize text styling (size, color), and drag the text to reposition it anywhere on the image canvas. 2. Workflow & Data Flow State Management & UI Control: Add text properties to EditorState: textContent: String, textSize: Float, textColor: Color, and textPosition: Offset (coordinates relative to the canvas). Add an "Add Text" button to the control panel that opens a text input dialog. Add controls (Sliders/Color rows) to dynamically update the text size and text color. Any change must trigger immediate UI recomposition. Canvas Interaction (Gestures): In the EditorScreen Canvas, overlay the text string on top of the image layers. Implement gesture detection (pointerInput with detectDragGestures) specifically targeted for the text layer. Single-finger drag must update textPosition in the ViewModel so the user can freely move the text around, while ensuring it does not conflict with the Erase/Restore brush gestures. Pipeline & Export Integration: Integrate the text layer into the rendering pipeline. The text must always respond instantly during real-time slider updates. Update BitmapComposer.generateFinalBitmap: When exporting the final sticker via MediaStoreSaver, render the text overlay as the absolute top layer (stamped directly on top of effectedBitmap) using the final textPosition mapped correctly to the high-res bitmap dimensions. 3. Output Provide clean, modular, and production-ready Kotlin code (ViewModel state updates, EditorScreen Canvas gesture updates, and BitmapComposer rendering logic). No explanations, just the code.